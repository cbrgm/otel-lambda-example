receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  awsemf:
  awsxray:

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [awsxray]
    metrics:
      receivers: [otlp]
      exporters: [awsemf]
